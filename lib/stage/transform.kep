/**
 * @fileOverview
 */
package transform
with 
    import '../transform' transformer,
    import 'bes::object' {setProperty}
in {


transform = \input#{options ast data} ->
    setProperty(
        input,
        'ast',
        transformer.transform(
            ast,
            (options.package_manager || 'amd'),
            data));

}