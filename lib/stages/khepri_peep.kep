/**
 * @fileOverview Khepri source optimization compiler stage.
 */
package optimize
with
    import '../khepri_peep' khepri_peep,
    import 'bes::object' {setProperty}
in {

/**
 * 
 */
optimize = \input#{tree data} ->
    setProperty(
        input,
        'tree',
        khepri_peep.optimize(tree, data));

}