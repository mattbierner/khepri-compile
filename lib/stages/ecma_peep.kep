/**
 * @fileOverview
 */
package optimize
with
    import '../ecma_peep' ecma_peep
in {

/**
 * 
 */
optimize = \{options tree} -> ({
    'options': options,
    'tree': ecma_peep.optimize(tree)
});

}