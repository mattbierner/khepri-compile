package Zipper
with
    import 'akh::identity' Identity,

    import './zippert' ZipperT
in {

/* Monad
 ******************************************************************************/
Zipper = ZipperT(Identity);

Zipper.run = \m ctx ->
    Identity.runIdentity(
        ZipperT.run(m, ctx));


}