package Zipper
with
    import 'akh::trampoline' Trampoline,

    import './zippert' ZipperT
in {

/* Monad
 ******************************************************************************/
Zipper = ZipperT(Trampoline);

Zipper.run = \m ctx ->
    Trampoline.run(
        ZipperT.run(m, ctx));

}